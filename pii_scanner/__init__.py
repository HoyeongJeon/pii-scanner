"""개인정보(PII) 노출 탐지 스캐너."""

# 리포트에 찍히는 도구 버전. 감사 산출물은 "언제·어느 버전으로 스캔했는지"가 남아야
# 나중에 결과를 재현하거나 탐지 규칙 변경 전후를 구분할 수 있다.
# pyproject.toml 의 version 과 같은 값이어야 하며 tests/test_version.py 가 둘을 묶어 둔다.
__version__ = "0.1.0"
